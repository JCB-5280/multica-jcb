---
title: CLI command reference
description: One-page overview of every top-level Multica CLI command. For full usage, run `mato <command> --help`.
---

import { Callout } from "fumadocs-ui/components/callout";

The Multica CLI mirrors almost everything the Web UI can do (create [issues](/issues), assign [agents](/agents), start the [daemon](/daemon-runtimes), and more). This page lists every top-level command with a one-line description. For the full set of flags and examples, run `mato <command> --help`.

## Getting authenticated

Run this the first time you use the CLI to obtain a **personal access token (PAT)**:

```bash
mato login
```

Your browser opens automatically. After you approve in the web app, the CLI saves the PAT (prefixed with `mul_`) to `~/.mato/config.json`. Every subsequent command authenticates with that PAT.

<Callout type="tip">
For CI or headless environments, skip the browser flow: create a PAT in the web app under **Settings → Personal Access Tokens**, then run `mato login --token <mul_...>` to supply it directly.
</Callout>

For the difference between token types, see [Authentication and tokens](/auth-tokens).

## Auth and setup

| Command | Purpose |
|---|---|
| `mato login` | Log in and save a PAT |
| `mato auth status` | Show current login status, user, and workspace |
| `mato auth logout` | Clear the local PAT |
| `mato setup cloud` | One-shot setup for Multica Cloud (login + install daemon) |
| `mato setup self-host` | One-shot setup for a self-hosted backend |

## Workspaces and members

| Command | Purpose |
|---|---|
| `mato workspace list` | List every workspace you can access |
| `mato workspace get <slug>` | Show details for one workspace |
| `mato workspace members` | List members of the current workspace |
| `mato workspace update <id> --name "..." [--description "..."] [--context "..."] [--issue-prefix "..."]` | Update workspace metadata (admin/owner). Long fields accept `--description-stdin` / `--context-stdin`. |

## Issues and projects

<Callout type="info">
`list` commands (`mato issue list`, `autopilot list`, `project list`, etc.) print short, copy-paste-ready IDs by default — issue keys like `MUL-123` for issues, short UUID prefixes for the rest. The `<id>` argument on the follow-up commands below accepts either the short ID or the full UUID, so the typical flow is `mato issue list` → copy the key → `mato issue get MUL-123`. Pass `--full-id` to a list command when you need the canonical UUID.
</Callout>

| Command | Purpose |
|---|---|
| `mato issue list` | List issues (prints copy-paste-ready issue keys) |
| `mato issue get <id>` | Show a single issue (accepts an issue key or a UUID) |
| `mato issue create --title "..."` | Create a new issue |
| `mato issue update <id> ...` | Update an issue (status, priority, assignee, etc.) |
| `mato issue assign <id> --agent <slug>` | Assign to an agent (triggers a task immediately) |
| `mato issue status <id> --set <status>` | Shortcut to change status |
| `mato issue search <query>` | Keyword search |
| `mato issue runs <id>` | Show agent runs on an issue |
| `mato issue rerun <id>` | Re-enqueue a fresh task for the issue's current agent assignee |
| `mato issue comment <id> ...` | Nested: view / post comments |
| `mato issue subscriber <id> ...` | Nested: subscribe / unsubscribe |
| `mato project list/get/create/update/delete/status` | Project CRUD |

## Agents and skills

| Command | Purpose |
|---|---|
| `mato agent list` | List the workspace's agents |
| `mato agent get <slug>` | Show an agent's configuration |
| `mato agent create ...` | Create an agent |
| `mato agent update <slug> ...` | Update an agent |
| `mato agent archive <slug>` | Archive |
| `mato agent restore <slug>` | Restore an archived agent |
| `mato agent tasks <slug>` | Show an agent's task history |
| `mato agent skills ...` | Nested: attach / detach skills |
| `mato skill list/get/create/update/delete` | Skill CRUD |
| `mato skill import ...` | Import a skill from GitHub, ClawHub, or the local machine |
| `mato skill files ...` | Nested: manage a skill's files |

## Squads

| Command | Purpose |
|---|---|
| `mato squad list` | List squads in the workspace |
| `mato squad get <id>` | Show a single squad |
| `mato squad create --name "..." --leader <agent>` | Create a squad (owner / admin) |
| `mato squad update <id> ...` | Update name, description, instructions, leader, or avatar |
| `mato squad delete <id>` | Archive (soft-delete) — transfers assigned issues to the leader |
| `mato squad member list/add/remove <squad-id>` | Manage squad members |
| `mato squad activity <issue-id> <action\|no_action\|failed> --reason "..."` | Used by squad leader agents to record an evaluation per turn |

See [Squads](/squads) for the full model.

## Autopilots

| Command | Purpose |
|---|---|
| `mato autopilot list` | List every autopilot in the workspace |
| `mato autopilot get <id>` | Show a single autopilot |
| `mato autopilot create ...` | Create an autopilot |
| `mato autopilot update <id> ...` | Update |
| `mato autopilot delete <id>` | Delete |
| `mato autopilot runs <id>` | Show run history |
| `mato autopilot trigger <id>` | Trigger a run manually |

## Daemon and runtimes

| Command | Purpose |
|---|---|
| `mato daemon start` | Start the daemon (background by default; add `--foreground` to run in the foreground) |
| `mato daemon stop` | Stop the daemon |
| `mato daemon restart` | Restart the daemon |
| `mato daemon status` | Check whether the daemon is online and its concurrency |
| `mato daemon logs` | View daemon logs |
| `mato runtime list` | List runtimes in the current workspace |
| `mato runtime usage` | Show resource usage |
| `mato runtime activity` | Recent activity log |
| `mato runtime update <id> ...` | Update a runtime's configuration |

## Miscellaneous

| Command | Purpose |
|---|---|
| `mato repo checkout <url>` | Clone a repo locally for agents to use |
| `mato config` | View or edit local CLI configuration |
| `mato version` | Print the CLI version |
| `mato update` | Upgrade the CLI to the latest release |
| `mato attachment download <id>` | Download an attachment from an issue or comment |

## Getting full flags

Every command supports `--help`:

```bash
mato issue create --help
mato agent update --help
```

v2 will ship a dedicated detailed reference page for each command.

## Next steps

- [Authentication and tokens](/auth-tokens) — PAT vs. JWT vs. daemon token
- [Daemon and runtimes](/daemon-runtimes) — how the `daemon` commands work under the hood
- [Creating and configuring agents](/agents-create) — all options for `mato agent create`
